<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iflytek.rpa.robot.dao.SharedVarUserDao">

    <insert id="insertBatch">
        INSERT INTO shared_var_user(shared_var_id, user_id, user_name, user_phone, deleted)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.sharedVarId}, #{entity.userId}, #{entity.userName}, #{entity.userPhone}, #{entity.deleted})
        </foreach>
    </insert>
    <select id="getAvailableSharedVarIds" resultType="java.lang.String">
        select
            shared_var_id
        from shared_var_user
        where deleted = 0 and user_id = #{userId}
    </select>

</mapper> 